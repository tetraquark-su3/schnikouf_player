cd "/home/tetraquark/Documents/quark_audio_player_v1.0/"

# Stage all deletions and additions in one go
git add -A

# Check what's about to be committed
git status

# Commit
git commit -m "v1.1 - bug fixes and improvements
- Reset default buttons, informations overlays, volume %, unormal behaviour with arrows
- VU meter: RMS-based levels with peak hold, smooth decay, richer gradient
- Spectrogram: resolution setting now wired through to widget
- Equalizer: state persisted across re-opens and track changes
- Engine: ffmpeg fallback when libsndfile missing in PyInstaller bundle"

git tag v1.1
git push origin main --tags
